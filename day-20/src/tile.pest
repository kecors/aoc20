alpha = { 'a' .. 'z' }
digit = { '0' .. '9' }

tile_id = { digit+ }
pixels_row = { ("#" | ".")+ }

heading = _{ "Tile " ~ tile_id ~ ":" }
tile = _{ heading ~ NEWLINE ~ (pixels_row ~ NEWLINE)+ }

main = _{ tile ~ (NEWLINE ~ tile)* }
